# H포스 (hpos.co.kr) — 작업 지침

**먼저 `작업노트.md` 를 읽을 것.** 브랜드·요금·지역 페이지 생성기·갤러리 등 배경이 거기 있다. 이 파일은 **매일 올리는 글(설치 후기)** 규칙만 정리한다.

## 사이트
- 정적 사이트(빌드 없음). GitHub Pages, 브랜치 **main** → https://hpos.co.kr (push 후 1~2분 반영)
- `blog/` 설치 후기·안내 글 (`YYYY-MM-DD-{지역slug}-vending.html`) · `blog.html`, `blog-2.html`… 목록(10편씩 페이지) · `photos/` 사진 · `region/` 지역 페이지 234곳(생성기 `_build/gen.ps1`)
- 상담 전화 010-6832-1994 (다른 바인 사이트와 동일). `.nojekyll` 없음 — `_build/`·`_content/` 는 Jekyll 규칙으로 게시 제외.

## 설치 후기 — 매일 1편 (2026-09-15부터 데일리 자동화가 씀. 2026-09-17 사용자 결정으로 2편→1편. 사이트관리/데일리/지시서/H포스.md 참고)
1. **지역**: `region/{시도}-{지역}.html` 중 아직 `blog/` 에 후기가 없는 지역(파일명에 지역 slug 로 확인). 최근 글들과 다른 시·도 우선. 한 바퀴 돌면 업종을 바꿔 다시.
2. **사진**: 원본 풀 `C:\Users\DOJIVERSE\Desktop\바인\자판기 사진` (픽포스·픽프리와 공용, 482장에 번호가 매겨진 `~/pickpos/_content/사진목록.tsv` 를 그대로 쓴다).
   ~~픽포스는 1번부터 올라가고, H포스는 482번부터 내려간다~~ → **2026-09-23 부터 번호 순서대로 쓰지 않는다**(한 폴더 "해옴" 사진이 엿새 이어져 같은 자판기로 보였다 — 사용자 지적). 다음 사진은 `powershell -NoProfile -ExecutionPolicy Bypass -File "C:/Users/DOJIVERSE/사이트관리/도구/자판기사진고르기.ps1" -Site H포스` 가 후보 5장을 골라 준다(하루 = 장소 폴더 하나, 최근 10일 안에 쓴 장소와 다른 두 사이트가 최근 3일 안에 쓴 장소 제외, 폴더 무작위·오래된 순, 세 사이트가 쓴 사진 재사용 없음). 1번부터 열어 확인하고 안 되면 다음 후보. 쓴 번호·건너뛴 번호는 `_content/사진사용기록.md` 에 지금 형식 그대로 적는다(도구가 이 표를 읽는다).
   사진은 Read 로 열어 확인 → 얼굴·상호·간판이 크게 나오면 건너뜀(기록에 "건너뜀"), 냉동 쇼케이스·정육 장비가 주인공이면 건너뜀. **사진에 보이는 공간에 업종을 맞춘다.**
   웹용 축소: 긴 변 1200px, JPEG 80, EXIF 회전 반영 → `photos/vendreal-{다음번호}.jpg` (PowerShell System.Drawing).
3. **글**: 가장 최근 후기를 복사해 전부 교체. 제목 `{지역} 무인자판기 설치 완료 — {업종} {특징}`, h2 4개(어떻게 구성했나 / 원격 관리·설치 / 구매·임대·렌탈 / 상담), CTA `tel:01068321994`, 하단 `bfoot`. 1,000~1,400자. 지역의 실제 상권·동 이름을 쓰되 **특정 업소명·매출·계약 사례는 지어내지 않는다.**
   - ⚠ **사진의 실제 장소와 글의 지역은 다르다.** `alt` 와 본문에 "{지역}에 설치한 현장" 이라고 쓰지 않는다(`"{업종} 무인자판기 설치 현장 - H포스"` 식). 글 하단 CTA 위에 반드시 한 줄: `<p class="note">※ 사진은 H포스가 시공한 설치 사례이며, 글에 적힌 지역의 현장 사진은 아닙니다.</p>`
4. **목록·사이트맵**: `_build/new-posts.json` 에 새 글 정보(f·img·alt·label·h·p·date)를 적고 `powershell -NoProfile -ExecutionPolicy Bypass -File _build\블로그갱신.ps1 -New _build\new-posts.json` → blog.html·blog-N.html 재페이지 + sitemap 등록. (예전 `add-posts-MMDD.ps1` 방식은 쓰지 않는다.)
5. 커밋 `자판기 후기 2편 발행({DATE}) - {지역}·{지역}` → push → 작업로그.

## 주의
- `_build/*.ps1` 은 UTF-8 BOM 으로 저장(PowerShell 5.1). 옛 스크립트들은 `Desktop\yoon` 경로가 박혀 있어 이 PC 에선 안 돈다 — 참고용.
- 공용 검색 부품 `search-kit.js` 는 8개 사이트 동일 사본(사이트관리/사이트대장.md 6절).
- 사용자는 코딩 초보자. 설명은 한국어로 쉽게.

## 데일리 시간대 규칙 (2026-09-18, 전 사이트 공통)
- 매일 **10:00** 자동화가 이 저장소에 데일리 글을 올린다. **09:30~13:00 에는 커밋·push 하지 않는다** — 자동화와 같은 파일(목록·사이트맵·RSS)을 동시에 고치면 꼬인다.
- 그 외 시간에는 손으로 글을 추가로 올려도 된다. 글 규칙(재탕 금지·사실 확인·정식 학교명·사진 라이선스·목록/사이트맵/RSS 갱신)은 데일리 지시서와 같다. 시작 전 `git pull`, 끝나면 push 까지.
- 자동화는 손으로 올린 글과 무관하게 정해진 편수를 올린다(글은 많을수록 좋다는 사용자 결정). 하루 목표 편수를 넘겨도 문제없다.

## 무인매장 페이지 (2026-09-22)
- `product/unmanned-store.html` = 바탕화면 `바인/자판기 사진/SAFE24제안서.pdf` 15쪽을 같은 순서로 옮긴 페이지(사용자 결정: PDF 문구 그대로, 회사명 "MTS 해원"만 제거, SAFE24 이름은 유지). 사진은 `photos/store/` (PDF 를 Windows.Data.Pdf 로 2배 렌더 후 잘라냄 — 이 PC 엔 python·poppler 없음).
- 상단 메뉴는 index.html 에만 있다(인테리어/철거 ↔ 설치 후기 사이).
